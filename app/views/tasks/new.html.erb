<h1><%= t('.new_task') %></h1>

<%= link_to t('attributes.task.task_index'), tasks_path %>

<!-- partial render -->
<%= render 'form', task: @task, action: :create, method: :post, button: t('.create_button') %>
